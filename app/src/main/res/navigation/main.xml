<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.designlife.opendraw.home.HomeFragment"
        android:label="fragment_home" />
    <fragment
        android:id="@+id/boardFragment"
        android:name="com.designlife.opendraw.board.BoardFragment"
        android:label="BoardFragment">
        <argument
            android:name="BoardId"
            app:argType="integer"
            android:defaultValue="-1"/>

        <argument
            android:name="BoardTitle"
            app:argType="string"
            android:defaultValue="White Board"/>

        <argument
            android:name="Created At"
            app:argType="integer"
            android:defaultValue="-1"/>

    </fragment>
</navigation>